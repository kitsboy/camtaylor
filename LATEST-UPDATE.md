# camtaylor — Last Updated 2026-09-24 by Buffy (M3)

Brief: The August remote line's worthwhile fixes were ported onto the local Sherpa line — Satohash provenance on every venture route, 44px touch targets, `100dvh` sizing, prefixed backdrop filters, a responsive axioms grid — while the locked voice and design stayed untouched. The ported device-QA matrix then caught three real phone defects (a horizontal scrollbar from the decorative glow orb, undersized filter and theme buttons, and Nostr cards that could not shrink below 320px), all fixed. The site is live on Cloudflare Pages at **https://camtaylor.pages.dev**, and the first public deploy exposed a launch blocker that is now fixed: the navbar badge, footer signal and contact note were hardcoded preview copy, so the header still read `LIVE / PRIVATE PREVIEW` on a public site. All three now branch on `IS_PRIVATE_PREVIEW`, and `npm run quality` fails the build if ungated preview copy returns.

**For Kimi — deployment ownership:** Buffy wants to own the camtaylor deployment going forward. Cloudflare Pages project `camtaylor` is the production target and `npm run deploy:live` is the only deploy command; **https://camtaylor.pages.dev is Buffy's deployment**. Kimi is understood to have her own deployment pipeline, so before anything else she should confirm which one is canonical in a handoff note — keep hers and Buffy stops deploying, or the Pages project is production and hers becomes staging/preview. Nothing attaches `camtaylor.ca`/`www` and nothing old gets deleted until that is settled. Full note in `docs/KIMI-HANDOFF.md` (Session — 2026-09-24).

Next for Cam: review the six seed dispatches, send one real contact-form test, then attach `camtaylor.ca` + `www` to the Pages project in the Cloudflare dashboard and delete the old WordPress records (see `docs/DEPLOYMENT.md` and `docs/PRIVATE-LAUNCH-GATE.md`). Also worth doing: set the GitHub default branch to `main` (`origin/HEAD` still points at `talent`).

Commit: `5137415b87fcc07ba9a34a5e600ec83be62c6461` on `main`, force-pushed to `origin/main` with approval (the August line is preserved on `m3/2026-09-24-publish` and `origin/talent`)

Uncommitted at time of writing: this note (`docs/KIMI-HANDOFF.md`, `LATEST-UPDATE.md`)
